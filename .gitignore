# Dependencies
node_modules

# Build output
.next/
dist
.dist
*.tsbuildinfo

# Environment variables.
# Ignore every .env variant, not just .env and .env*.local: `vercel env pull`
# writes real credentials to whatever filename you give it, and one `git add -A`
# on a public repo is all it takes.
.env*
!.env.example

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store

# Vercel
.vercel

# Old Vite files (if any)
vite.config.js
vite.config.d.ts